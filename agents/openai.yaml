interface:
  display_name: Firecracker
  short_description: One shared spark for project docs, decisions, and cleanup
  default_prompt: Use $firecracker to inspect this project’s documents, fix evidence-backed
    inconsistencies, and recommend cleanup while distinguishing observed agent access
    from unknown usage.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
  brand_color: '#FF6338'
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
