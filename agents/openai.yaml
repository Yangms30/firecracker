interface:
  display_name: Firecracker
  short_description: One shared spark for project docs, decisions, and cleanup
  default_prompt: "Use $firecracker to map this project's documents and implementation, report evidence-backed inconsistencies and cleanup recommendations, and propose next steps without editing files."
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
  brand_color: '#FF6338'
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
